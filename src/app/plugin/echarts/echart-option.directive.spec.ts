import {EchartOptionDirective} from './echart-option.directive';

describe('EchartOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartOptionDirective();
    expect(directive).toBeTruthy();
  });
});
