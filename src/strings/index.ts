import capitalize from "strings/capitalize"
import capitalizeEveryWord from "strings/capitalizeEveryWord"
import csvDownload from "strings/csvDownload"
import escapeHTML from "strings/escapeHTML"
import escapeRegExp from "strings/escapeRegExp"
import firstUniqChar from "strings/firstUniqChar"
import fromCamelCase from "strings/fromCamelCase"
import getURLParams from "strings/getURLParams"
import htmlToExcel from "strings/htmlToExcel"
import isAbsoluteURL from "strings/isAbsoluteURL"
import maskStr from "strings/maskStr"
import parseCookie from "strings/parseCookie"
import randomHexColorCode from "strings/randomHexColorCode"
import serializeCookie from "strings/serializeCookie"
import sortCharactersInString from "strings/sortCharactersInString"
import splitLines from "strings/splitLines"
import unescapeHTML from "strings/unescapeHTML"

export default {
  capitalize,
  capitalizeEveryWord,
  csvDownload,
  escapeHTML,
  escapeRegExp,
  firstUniqChar,
  fromCamelCase,
  getURLParams,
  htmlToExcel,
  isAbsoluteURL,
  maskStr,
  parseCookie,
  randomHexColorCode,
  serializeCookie,
  sortCharactersInString,
  splitLines,
  unescapeHTML,
}
